(0, 500, '{"error":"output-head logits contained non-finite values"}\n')
(1, 500, '{"error":"output-head logits contained non-finite values"}\n')
(2, 500, '{"error":"output-head logits contained non-finite values"}\n')
(3, 500, '{"error":"output-head logits contained non-finite values"}\n')
(4, 500, '{"error":"output-head logits contained non-finite values"}\n')
(5, 500, '{"error":"output-head logits contained non-finite values"}\n')
(6, 500, '{"error":"output-head logits contained non-finite values"}\n')
(7, 500, '{"error":"output-head logits contained non-finite values"}\n')
(8, 500, '{"error":"output-head logits contained non-finite values"}\n')
(9, 500, '{"error":"output-head logits contained non-finite values"}\n')
(10, 500, '{"error":"output-head logits contained non-finite values"}\n')
(11, 500, '{"error":"output-head logits contained non-finite values"}\n')
(12, 500, '{"error":"output-head logits contained non-finite values"}\n')
(13, 500, '{"error":"output-head logits contained non-finite values"}\n')
(14, 500, '{"error":"output-head logits contained non-finite values"}\n')
(15, 500, '{"error":"output-head logits contained non-finite values"}\n')
(16, 500, '{"error":"output-head logits contained non-finite values"}\n')
(17, 500, '{"error":"output-head logits contained non-finite values"}\n')
(18, 500, '{"error":"output-head logits contained non-finite values"}\n')
(19, 500, '{"error":"output-head logits contained non-finite values"}\n')
(20, 500, '{"error":"output-head logits contained non-finite values"}\n')
(21, 500, '{"error":"output-head logits contained non-finite values"}\n')
(22, 500, '{"error":"output-head logits contained non-finite values"}\n')
(23, 500, '{"error":"output-head logits contained non-finite values"}\n')
(24, 500, '{"error":"output-head logits contained non-finite values"}\n')
(25, 500, '{"error":"output-head logits contained non-finite values"}\n')
(26, 500, '{"error":"output-head logits contained non-finite values"}\n')
(27, 500, '{"error":"output-head logits contained non-finite values"}\n')
(28, 500, '{"error":"output-head logits contained non-finite values"}\n')
(29, 500, '{"error":"output-head logits contained non-finite values"}\n')
(30, 500, '{"error":"output-head logits contained non-finite values"}\n')
(31, 500, '{"error":"output-head logits contained non-finite values"}\n')
